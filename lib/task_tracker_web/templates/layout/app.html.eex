<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>TaskTracker · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body>
    <div class="container-fluid">
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark row">
        <%= if @current_user do %>
	        <%= link "Tasks", to: Routes.task_path(@conn, :index), class: "navbar-brand nav-text" %>
	      <% else %>
	        <%= link "Tasks", to: Routes.page_path(@conn, :index), class: "navbar-brand nav-text" %>
	      <% end %>

        <ul class="navbar-nav">
	        <li class="nav-item">
	          <%= link "New Task", to: Routes.task_path(@conn, :new), class: "nav-text" %>
	        </li>
        </ul>

        <div class="ml-auto nav-text">
          <%= if @current_user do %>
            <%= link @current_user.username, to: Routes.user_path(@conn, :show, @current_user.id), class: "nav-text" %> |
            <%= link "Logout", to: Routes.session_path(@conn, :delete), method: :delete, class: "btn btn-warning" %>
          <% end %>
        </div>
      </nav>

      <div class="row">
        <%= render @view_module, @view_template, assigns %>
      </div>
    </div>

    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
